{% extends 'base.html.twig' %}

{% block title %}Hello CitationController!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Citation aléatoire :</h1>

    <span>
        {{ citation.getText() }} <br>
        {{ citation.getBook().getTitle() }}, {{ citation.getAuthor().getName() }}
    </span>
</div>
{% endblock %}
